when:
  - event: ["push", "manual"]
    branch: ["main"]
  - event: ["pull_request"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs:
    - nodejs_22
    - pnpm_11

steps:
  - name: "Install dependencies"
    command: "pnpm install --frozen-lockfile"
  - name: "Lint"
    command: "pnpm exec vp check"
  - name: "Test"
    command: "pnpm exec vp run -r test"
