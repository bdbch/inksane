when:
  - event: ["push", "pull_request"]
    branch: ["main"]

engine: microvm
image: nixos

dependencies:
  - pnpm
  - github:ryoppippi/nix-vite-plus

steps:
  - name: "Install dependencies"
    command: pnpm install

  - name: "Run Linter"
    command: "vp check"
